<h1> Your Teams: </h1>
<ol>
  <h3 class="indent">
    <% current_user.teams.each do |team| %>
      <li><%= link_to team.name, team_path(team) %></li>
    <% end %>
  </h3>
</ol>

<%= link_to "New Team", new_team_path, class: "button-primary" %>
